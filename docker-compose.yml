version: '3.8'

services:
  spring-app:
    build:
      context: ../
      dockerfile: Dockerfile
    container_name: spring-app
    ports:
      - "8090:8090"
    volumes:
      - ./logs:/app/logs
    depends_on:
      - mysql

volumes:
  es_data:
  mysql_data: