version: '3.8'

services:
  spring-app:
    image: spring-app-image:latest
    container_name: spring-app
    ports:
      - "8090:8090"
    volumes:
      - ./logs:/app/logs

volumes:
  es_data:
  mysql_data:
